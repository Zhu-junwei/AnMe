{
  "name": "anme-userscript",
  "version": "10.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  }
}
