{
  "name": "anme-browser-extension",
  "version": "10.0.11",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:extension",
    "build:extension": "node scripts/build-extension.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  }
}
